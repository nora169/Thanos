from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='"it's delete half images numbers in a folder in a random way"',
    author='me',
    license='',
)
